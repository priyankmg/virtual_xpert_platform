import AdminMetricsPage from '../admin-metrics/page';

/** Alias route — same Admin Engagement Metrics UI as `/admin-metrics`. */
export default function AdminPage() {
  return <AdminMetricsPage />;
}
